import 'package:flutter/material.dart';

class WsLoader extends StatelessWidget {
  const WsLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
